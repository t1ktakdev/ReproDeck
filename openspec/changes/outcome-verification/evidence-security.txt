Outcome Verification security evidence

- No new privileged execution paths introduced.
- Uses existing Runner/Timeline/Evidence/Permissions systems; permission Ask/Deny semantics preserved.
- DB migration adds tables only; foreign keys reference existing sessions and evidence tables.
- Tests include recovery and evidence storage behaviors inherited from timeline/evidence modules.

- Manual checks performed:
  - Ran cargo fmt/clippy/tests via quality gate.
  - Verified sanitization behavior remains in timeline.finish_execution (stdout/stderr redaction).

Recommendations / mitigations:
- Ensure evidence_links table only references existing evidence entries created via accepted evidence APIs.
- Do not add automatic destructive rerun logic; follow permission engine for Ask/Deny.
